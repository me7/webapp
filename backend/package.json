{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.imba",
    "dist": "imba build --esm src/server.imba && bun build --compile dist/server.mjs --outfile ../dist/server"
  },
  "devDependencies": {
    "bimba-cli": "^0.2.1",
    "bun-types": "^1.1.4",
    "imba": "^2.0.0-alpha.235"
  },
  "dependencies": {
    "@elysiajs/static": "^1.0.2",
    "elysia": "^1.0.14"
  }
}